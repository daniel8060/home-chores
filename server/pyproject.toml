[project]
name = "chore-tracker-server"
version = "0.1.0"
description = "Backend API for the Chore Tracker app"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
]
